<%= form_for @user,:url=> admin_user_path(@user) do |f|%>

  <div class="form-group">
    <%= f.label :role, "權限調整"%>
    <%= f.select :role, [ ["無",""], ["管理員", "admin"] ] %>
  </div>
  
<%= f.submit "送出",:class => "btn btn-primary" %>
<%= link_to "回到上一頁",admin_categories_path,:class => "btn btn-default"%>
<%end%>